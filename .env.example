# Mapping API key → organization_id (JSON dict)
API_KEYS={"your-secret-key": 1}

# Legacy single API key (deprecated, use API_KEYS instead)
# API_KEY=your-secret-key

# SQLite (default)
# DATABASE_URL=sqlite:///./database.db

# PostgreSQL
DATABASE_URL=postgresql+psycopg://user:password@localhost:5432/dbname

# Clerk secret key for password verification
CLERK_SECRET_KEY=sk_live_xxxx
